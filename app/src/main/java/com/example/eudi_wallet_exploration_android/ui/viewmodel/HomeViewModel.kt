package com.example.eudi_wallet_exploration_android.ui.viewmodel

/**
 * An `AndroidViewModel` that exposes a `StateFlow<List<Credential>> of stored credentials. Calls
 * CredentialRepository.getAll()` on init and refreshes after an credential is added or deleted.
 * Provides a `deleteCredential(id: String)` method.
 */
class HomeViewModel {
}
