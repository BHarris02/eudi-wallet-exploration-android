package com.example.eudi_wallet_exploration_android.ui.screen

/**
 * Composable that observes `PresentCredentialViewModel.state` and renders each state &mdash;
 * idle, consent required (delegates to ConsentScreen), presenting, success, and error.
 */
