package com.example.eudi_wallet_exploration_android.ui.screen

/**
 * Composable that observes `ReceiveCredentialViewModel.state`. Shows a button to trigger the
 * credential offer flow (hardcoded URL for testing), a loading indicator during the flow, and a
 * success or error message on completion.
 */
