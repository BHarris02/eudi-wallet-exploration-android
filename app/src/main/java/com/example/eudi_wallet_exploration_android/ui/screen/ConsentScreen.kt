package com.example.eudi_wallet_exploration_android.ui.screen

/**
 * Composable shown before a presentation is submitted. Displays the Verifier's identity, the
 * claims that will be shared, and Accept/Decline buttons. Wired to
 * `PresentCredentialViewModel.onConsentGranted()` and `onConsentDeclined()`.
 */
