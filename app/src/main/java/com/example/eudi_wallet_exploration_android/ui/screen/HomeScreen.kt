package com.example.eudi_wallet_exploration_android.ui.screen

/**
 * Composable that observes `HomeViewModel.credentials`. Shows an empty state when no credentials
 * are stored, or a list of credential cards. Each card navigates to `CredentialDetailScreen` on
 * tap. Stateless beyond what it receives from the ViewModel.
 */
