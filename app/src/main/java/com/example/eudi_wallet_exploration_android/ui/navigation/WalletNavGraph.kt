package com.example.eudi_wallet_exploration_android.ui.navigation

/**
 * Defines the Compose navigation graph. Routes: `home`, `receive`, `detail/{credentialId},
 * `present`. Deep link intents for `openid-credential-offer://` and `openid4vp://` are
 * registered here and routed to the appropriate screens.
 */
