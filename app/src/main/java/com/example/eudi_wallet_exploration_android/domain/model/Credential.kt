package com.example.eudi_wallet_exploration_android.domain.model

/**
 * A data class representing a stored credential. Contains the raw JWT string, the decoded issuer,
 * and a map of decoded claims. This is the wallet's internal domain representation.
 */
data class Credential()
