package com.example.eudi_wallet_exploration_android.domain.model

/**
 * A data class representing a parsed presentation request received from a Verifier. Contains the
 * `nonce`, `responseUrl`, and a list of required claims paths extracted from the
 * `presentation_definition`.
 */
data class PresentationRequest()
