package com.example.eudi_wallet_exploration_android.data.api

/**
 * A class wrapping Ktor Client calls to the Verifier backend. Exposes:
 *
 * - `submitPresentation(responseUri: String, vpToken: String): Result<Unit>
 */
class VerifierApiService {
}