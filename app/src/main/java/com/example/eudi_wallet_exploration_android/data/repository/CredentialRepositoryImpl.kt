package com.example.eudi_wallet_exploration_android.data.repository

import com.example.eudi_wallet_exploration_android.domain.repository.CredentialRepository

/**
 * The concrete implementation class of the `CredentialRepository` interface exposed in `:domain`.
 */
class CredentialRepositoryImpl(): CredentialRepository {
}